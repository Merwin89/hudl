How to run

-- Install Cypress : npm install Cypress
-- Clone the repo
-- In the terminal set > source .env
-- Then hit either of the following commands to run the script > npm cypress run OR npm cypress open
-- The test cases are inside cypress<e2e<login.cy.js
-- The page elements and functions are inside cypress<pages<loginpage.js